<h1>Create a New Workout</h1>

<%= form_for @workout do |f| %>

  <div>
    <%= f.label :label %>
    <%= f.text_field :label %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

  <% end %>

<%= link_to 'Back', workouts_path %>
