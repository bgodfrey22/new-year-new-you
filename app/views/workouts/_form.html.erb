<%= form_for @workout do |f| %>
  <% if workout.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(workout.errors.count, "error") %> prohibited this exercise from being saved:</h2>

    <ul>
    <% workout.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div>
    <%= f.label :label %>
    <%= f.text_field :label %>
  </div>

  <div>
    <%= f.label :exercise %>
    <%= f.fields_for @workout.exercises do |exercise| %>
          <%= collection_select(:exercises_workout, :exercise_id, Exercise.all, :id, :name) %>
    <% end %>
  </div>

  <%= hidden_field :exercises_workout, :workout_id, value: workout.id %>
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
